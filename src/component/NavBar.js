import { AppBar } from "@mui/material";
import React from "react";

export const NavBar = () => {
  return (
    <AppBar position="static" className="navbar">
      <h2>Shopsey</h2>
    </AppBar>
  );
};
